# Decisions Log

| Date | Decision | Reason |
|------|----------|--------|
| 2026-08-04 | React Router template (shopify-app-template-react-router), not Remix | shopify.dev now marks it "the recommended path for most apps"; TS + Prisma/SQLite built in; API version pinned 2025-10 |
| 2026-08-04 | Scaffolded via direct git clone instead of `shopify app init` | CLI 3.84 forces interactive Shopify auth during init (creates the app in the Partner org up front); clone is deterministic, app link happens at first `shopify app dev` |
| 2026-08-04 | Run node via `/usr/local/opt/node@22/bin` (22.23.1) PATH prefix | Default node 22.9.0 falls inside template's excluded engine range (`>=22 <22.12`); didn't relink system node without asking |
| 2026-08-04 | Kept template's AGENTS.md, dropped its CLAUDE.md | Our CLAUDE.md is the project master prompt; AGENTS.md carries the same Shopify AI Toolkit pointer |
| 2026-08-04 | Commit package-lock.json (template gitignores it) | Template stays package-manager-agnostic; we're npm-only and want reproducible installs for CI/Docker |
| 2026-08-10 | Manual Billing API (appSubscriptionCreate via package `billing` config), not Shopify Managed Pricing | shopify.dev now recommends Managed Pricing as default for new App Store apps, but manual is confirmed available and non-deprecated on 2025-10. Manual keeps plan gating + trials fully in code, testable now without Partner Dashboard access (store link still pending). Gating reads active subscriptions, so switching to Managed Pricing later only swaps the purchase flow — flag for owner review before submission |
| 2026-08-10 | Billing test mode: test charges outside production by default; `BILLING_TEST_MODE=true\|false` env override wins either way | Dev stores can't be charged real money; production must default to real charges. Override supports demoing real flow in dev-like envs and test charges on a production deploy during app review |
| 2026-08-10 | Plan/feature logic lives in `app/models/plans.ts` (shared, no `.server` suffix) | Plan metadata renders in the client (plan page); React Router build forbids `.server` imports from components. Nothing secret in plan names/prices/features. Billing calls stay in `billing.server.ts` |
| 2026-08-10 | Free tier = no Shopify subscription at all; plan derived as highest-ranked active subscription name | Shopify has no $0 subscription concept worth carrying; `resolvePlan` ignores unknown names and tolerates transient double-subscriptions mid-upgrade (replacementBehavior STANDARD) |
| 2026-08-10 | 404 beacon inlined in the app-embed liquid (no asset file), sendBeacon with fetch fallback | ~0.4KB inline beats an extra asset request; sendBeacon is async/fire-and-forget and survives navigation; wrapped in try/catch so storefront can never break. Proxy subpath /apps/pathmend hardcoded in the snippet — revisit if merchants can customize subpaths |
| 2026-08-10 | Capture rate limit: in-memory per-shop bucket, 120 events/min, silent drops | Crawler storms on migrated sites can 404 thousands of times/min; DB safety beats tail completeness. Per-process only — documented to move to shared store if app scales horizontally |
| 2026-08-10 | 404 dedupe key: lowercased, fragment-stripped, trailing-slash-trimmed path (query kept) | Case/slash variants of one dead URL should be one log row with hit counts; query strings kept because they distinguish real old-site URLs in migrations |
| 2026-08-10 | Repeat 404 on a "resolved" event reopens it to "unresolved"; "ignored" never auto-reopens | Resolved+recurring means the redirect regressed (supports the "never silently disappear" promise); ignored is an explicit merchant choice we must respect |
| 2026-08-10 | Stored `referrer` (1024-char cap) is out of scope for customers/data_request & customers/redact | It's page-level web-standard referrer data with no linkage to any Shopify customer record — the app stores zero customer PII, so those webhooks correctly no-op. Shop-level cleanup (shop/redact + app/uninstalled) purges all NotFoundEvent rows including referrers; documented here for App Review |
| 2026-08-10 | CSV import applies per-row via createRedirect (500 rows/file cap), not Shopify's native urlRedirectImportCreate/Submit | Native bulk import verified to exist on 2025-10 (staged upload + async submit) but gives no per-row error report, no DB mirror, no 404 auto-resolution, no source attribution, and needs polling. Per-row keeps dry-run/error-report quality high at migration-typical sizes; native import is the documented upgrade path if >10k-row imports become a real need |
| 2026-08-10 | Plain CSV import/export ungated (free-plan 25-redirect cap still applies); migration importer (auto-match + confidence) stays MIGRATION-gated | FREE spec text doesn't exclude CSV; the cap already limits value extraction, and easy import drives adoption. The paid differentiator is the matching intelligence, not file upload |
| 2026-08-10 | CSV parser hand-rolled (~60 lines), no papaparse dependency | Two known columns, no multi-line quoted fields (URLs can't contain newlines), extra columns ignored for WordPress/BigCommerce export compatibility; 12 unit tests cover quoting/BOM/CRLF/dupes. A dependency would exceed the problem |
| 2026-08-10 | Pattern rules match the WHOLE path, case-insensitively, for both wildcard and regex kinds | Predictable exact-match semantics mirroring Shopify's redirect matching; partial-match regex would surprise merchants and widen the auto-heal blast radius |
| 2026-08-10 | Merchant regex ReDoS gate = redos-detector analyzer (pure-JS, server-only), after 3 hand-rolled heuristics each got bypassed | Rounds 2/3/4 each bypassed a bespoke heuristic by a sibling shape: nested alternation ((a\|a)), nested-optional ((a?){30}), then ambiguous internal partition ((a+a)+, (.*a)+ — 72s at 33 chars, defeats the last-token-quantified scanner). The general condition is character-set-overlap analysis, too complex to hand-roll safely (reviewer: "plan as its own task, not a 4th ad-hoc patch"). Resolution: redos-detector (deps: regjsparser only — no native build, unlike RE2) analyzes the automaton; verified across ~50 adversarial patterns that it rejects all bypass shapes (incl. all 4 rounds' bypasses) and passes legit URL patterns, with no accepted-but-slow pattern found. Runs server-side ONLY (regex-safety.server.ts, gated in createPatternRule) so it never enters the client bundle — confirmed absent from build/client. The pure paren-scanner stays as a cheap client-preview pre-filter; the 200-char match cap stays as belt-and-suspenders |
| 2026-08-10 | redos-detector analysis timeout = 250ms, and it fails CLOSED (timeout → safe:false) | 1000ms made the test suite exceed vitest's 5s per-test limit (several unsafe patterns each burning ~1s). Verified redos-detector returns safe:false on timeout, so a shorter budget can only reject MORE, never admit a dangerous pattern — 250ms classifies real URL patterns fine and bounds worst-case rule-creation latency. Unsafe-pattern tests use test.each so no single test() accumulates multiple analysis timeouts |
| 2026-08-10 | Migration importer ships BOTH modes; sitemap fetch is SSRF-hardened (user-chosen over CSV-only or upload-only) | Full spec coverage. SSRF layers: https-only + no-credentials + public-host allowlist (ssrf.server.ts), DNS-resolve-then-recheck-every-IP (rebinding guard), redirect:manual (no chasing 3xx to internal hosts), Content-Length + streaming byte cap, AbortController timeout. isBlockedHost covers loopback/private/link-local/CGNAT/metadata/IPv6-ULA/IPv4-mapped + numeric shorthands (127.1, decimal, hex). DNS/fetch injected for unit tests |
| 2026-08-10 | Matching heuristic: exact-handle=high, slugified-title=medium, ≥50% token overlap=low; storefront paths built from handles by convention | Handles are the storefront slug; conventional paths (/products/, /collections/, /pages/, /blogs/<blog>/<article>) avoid needing an onlineStoreUrl field (not exposed on these objects on 2025-10, verified). Confidence tiers let merchants trust/review/skip; low matches are shown but not default-approved |
| 2026-08-10 | Migration UrlMatch type + DEFAULT_MAX_MATCH_URLS live in pure migration.ts, not the .server module | The review table (client) needs the type and the cap; React Router build forbids importing a .server module from a component. Server module re-exports them for its own callers |
| 2026-08-11 | SSRF host classification delegates to ipaddr.js (+ manual IPv4-compatible-IPv6 collapse), not hand-rolled regex | Security review proved ::ffff:7f00:1 (hex-group IPv4-mapped) reached a real loopback socket via fetch — the hand-rolled isBlockedIpv6 regex only matched the dotted-quad ::ffff:a.b.c.d form. Same root cause as the ReDoS/open-redirect rounds: don't hand-roll canonicalization. Now an IP literal is allowed ONLY if ipaddr.js range()==="unicast". ipaddr.js doesn't special-case IPv4-COMPATIBLE (::a.b.c.d, deprecated but still resolved — ::7f00:1 == ::127.0.0.1, which it calls unicast), so ipRange() collapses any `::`/`::ffff:` low-32-bit-embedded form to its IPv4 first. Plus trailing-dot strip (127.0.0.1./localhost. bypass). ipaddr.js is pure-JS zero-dep, server-only (confirmed absent from client bundle) |
| 2026-08-11 | Sitemap DNS-rebinding: strip IPv6 brackets before resolve; accept the residual connect-time TOCTOU rather than IP-pin | Handing url.hostname (bracketed for IPv6) to resolve() made the rebinding guard depend on a resolver quirk. Fixed by stripping brackets so it fails closed. Not IP-pinning the connection: fetch to https://<IP> validates the cert against the IP, not the Host header, breaking TLS for real sites. Residual TOCTOU (global fetch re-resolves at connect) needs attacker-controlled authoritative DNS with sub-second rebind timing against their own migration target; redirect:manual + https-only + resolve-recheck cover the common vectors. Documented in migration.server.ts |
| 2026-08-11 | Migration search slug restricted to [a-z0-9-] before building Shopify search query | Old-URL slugs are attacker-influenced (sitemap/CSV); stripping non-handle chars prevents Shopify search-syntax operator injection (:, ", *). Same-tenant only even unsanitized, but predictable + robust |
| 2026-08-11 | Sitemap index followed one level (≤20 children), each child fetched through the SSRF guard | Large stores ship a sitemap index of child sitemaps; without recursion the importer would try to match .xml URLs and find nothing. Child locs are attacker-influenced too, so each goes through fetchSitemapText's full guard; one bad child is skipped, not fatal |
| 2026-08-11 | Analytics "404s over time" buckets by firstSeenAt, not raw per-hit timestamps | We store one deduped (shop,path) row with a hit counter + first/last seen, not a row per hit — so the time series answers "new broken URLs per day" not "hit volume per day". Storing per-hit rows would balloon the DB against the beacon rate limit for marginal charting value; documented in analytics.ts so the metric isn't mistaken for hit volume |
| 2026-08-11 | Analytics chart is inline SVG, no charting library | Spec demands zero bundle bloat; a 30-bar day chart is ~15 lines of SVG. Avoids a dependency (and its client-bundle weight) for something trivial. Attacker-controlled 404 paths never enter the chart (it plots date/count only); the top-paths table renders paths as React text nodes |
| 2026-08-11 | Weekly digest card is NOT plan-gated (lives on the free Dashboard); analytics PAGE is PRO-gated | The digest is the shop's own 3-number weekly summary — no cross-tenant data, low value, drives engagement/upgrade. The full analytics page (charts, top paths, history) is the paid feature. Email digests remain post-launch per spec §4 |
| 2026-08-11 | Analytics status totals via DB groupBy; chart/top-paths via a bounded (take 5000, window-filtered) fetch | Code review flagged the original loadAnalytics as an unbounded findMany of every distinct path. groupBy computes exact status counts + hit sums server-side with no rows loaded; the windowed fetch caps in-memory rows for the chart/top-N. A shop with >5000 distinct paths in the window still gets correct aggregate totals, just a sampled chart. summarizeAnalytics refactored to take pre-aggregated buckets instead of rows |
| 2026-08-11 | Onboarding step completion is evidence-based, not a stored flag; theme-editor deep-link falls back gracefully | A captured 404 proves the embed is live (steps 1+2); a redirect proves first value (step 3) — no new state to persist or drift. Deep-link needs the extension UUID (SHOPIFY_THEME_EXTENSION_ID, only known post-deploy); falls back to the plain editor URL, then /app, so it never renders a broken link pre-deploy |
| 2026-08-11 | Microcopy: applied the jargon/pluralization/dead-end/badge fixes from the audit; DECLINED renaming spec-defined nav items | "Settings & Plan", "Pattern rules & auto-heal", and "404 Log" are named per CLAUDE.md §7's defined IA — kept. "404" is acceptable merchant vocabulary. Fixed the softer leaks (app embed/capture embed → "404 tracking", "healed" → "Fixes made", raw enum badges → capitalized, `(s)` → real pluralization). SitemapUrlError class distinguishes merchant-safe guard messages from raw network/parse errors so only the former surface to merchants |
| 2026-08-10 | Redirect open-redirect gate resolves the target against a probe origin (WHATWG parser), not a character blocklist | Security HIGH reused validateRedirectInput (hand-typed) for machine-substituted targets. Blocklist patching got bypassed twice: "/\evil.com" (round 2, \→/ normalization) then "/<tab>/evil.com" (round 3, WHATWG strips tab/LF/CR). Final: `relativeTargetEscapesOrigin` does `new URL(target, probe).origin !== probe` — delegates ALL normalization to the spec parser, closing the whole escape-trick class at once (verified: rejects \, //, tab/LF/CR variants; accepts absolute https and normal paths). Applied at all three call sites (validateRedirectInput, isSafeTargetShape, matchPatternRule capture clamp). hasEmbeddedUrl kept as a separate tidiness rule for same-origin-but-URL-shaped paths (/news/https://evil.com) |
| 2026-08-10 | Auto-heal checks entitlement per matched 404 via one lazy GraphQL call (getPlanViaAdmin), after the rule-match test | Keeps the beacon hot path DB-only (no API call for the 99% no-rules/no-match case) while guaranteeing downgraded shops' rules stop healing immediately — no stale plan snapshots to invalidate |
